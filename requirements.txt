# Core API
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.4

# Retrieval + ML (CPU-only, deterministic)
scikit-learn==1.3.2
numpy==1.26.4

# Optional local-LLM call (graceful fallback when unavailable)
httpx==0.28.1

# Logging
loguru==0.7.2

# Testing
pytest==8.4.2
pytest-asyncio==1.3.0
